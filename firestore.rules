rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 1. Global Safety Net
    match /{document=**} {
      allow read, write: if false;
    }

    // 2. Application state synchronization path
    match /app/state {
      allow read, write: if true;
    }
  }
}
